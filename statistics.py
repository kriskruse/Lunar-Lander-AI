import numpy as np

rewards = np.load('')
time_taken = np.load('')
won = np.load('')
lost = np.load('')
end_position = np.load('')

def confidence_interval(array):
    mean = np.mean(array)
    critical_value =
